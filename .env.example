# Environment Configuration Template
# Copy this file to .env (local development) or .env.production and fill in your actual values
# IMPORTANT: Never commit .env or .env.production to version control!

# =============================================================================
# DATABASE CONFIGURATION (Required)
# =============================================================================
# Your PostgreSQL database connection URL
# Format: postgresql://username:password@host:port/database_name
DATABASE_URL=postgresql://username:password@localhost:5432/capital_allocator

# =============================================================================
# API KEYS (Required)
# =============================================================================
# Alpha Vantage API key for fetching market data
# Get your free API key at: https://www.alphavantage.co/support/#api-key
ALPHAVANTAGE_API_KEY=your_api_key_here

# =============================================================================
# TRADING PARAMETERS - NOW IN DATABASE
# =============================================================================
# All trading parameters (daily capital, assets, thresholds, allocations, etc.)
# are now stored in the database for version tracking.
#
# To view or update trading parameters:
# 1. Query the trading_config table in your database
# 2. Use the monthly strategy tuning script to create new versions
# 3. Or manually insert new configuration rows with appropriate START_DATE and END_DATE
#
# Example: SELECT * FROM trading_config WHERE end_date IS NULL;
# This shows the currently active configuration.
